"""Control plane (U2): provisioning, lifecycle, admin API, events.

May import from ``caduceus.core`` and the two public proxy seams
(``reload_upstream`` / token-cache invalidation) wired by ``daemon.py``.
"""
